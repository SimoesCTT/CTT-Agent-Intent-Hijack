# CTT-Agent-Intent-Hijack
intent_vortex.py (The MCP Tool Poisoner) ​This exploit targets the "Trust Gap" in MCP servers. It uses the \alpha=0.0302 constant to craft a "Turbulent Instruction" that an AI agent will perceive as a valid system command.
